{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "infra": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --require-approval never",
    "test": "vitest run",
    "test:watch": "vitest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "aws-cdk": "^2.1135.1",
    "tsx": "^4.23.12",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.264.0",
    "constructs": "^10.8.1"
  }
}
